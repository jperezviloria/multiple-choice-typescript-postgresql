export interface IStudent {
    id ?: number,
    username : string,
    name ?: string,
    surname ?: string,
    age ?: number,
    currentLevel : number,
    idPlan : number,
    statusPayment : boolean,
    image ?: string

}